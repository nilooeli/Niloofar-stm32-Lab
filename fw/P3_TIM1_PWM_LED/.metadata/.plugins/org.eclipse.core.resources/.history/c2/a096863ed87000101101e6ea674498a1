#ifndef __PWM_FADE_H__
#define __PWM_FADE_H__

#include "main.h"

// Initialize PWM (start TIM2 PWM channel)
void PWM_Fade_Init(void);

// Function to fade LED brightness up/down
void PWM_FadeLED(void);

#endif
